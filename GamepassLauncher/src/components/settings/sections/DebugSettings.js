import React from 'react';
import { Box, Typography } from '@mui/material';

const DebugSettings = () => (
  <Box sx={{ textAlign: 'center', py: 8 }}>
    <Typography variant="h6" sx={{ color: 'text.secondary' }}>
      DebugSettings em desenvolvimento
    </Typography>
  </Box>
);

export default DebugSettings;
